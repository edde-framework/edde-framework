<?php
	declare(strict_types = 1);

	namespace Edde\Api\Cache;

	/**
	 * General interface marking objects which can be save into the cache (thus they're serializable).
	 */
	interface ICacheable {
	}
